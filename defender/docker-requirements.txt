--extra-index-url https://download.pytorch.org/whl/cpu

# Compatible with Python 3.11 for both MalConv and EMBER2024
Flask==3.0.0
gevent==23.9.1
envparse==0.2.0
numpy==1.26.0

# PyTorch for MalConv (CPU-only for container efficiency)
torch==2.1.0+cpu

# LightGBM and EMBER2024 for thrember features (updated to meet thrember requirements)
lightgbm>=4.5.0
git+https://github.com/FutureComputing4AI/EMBER2024.git

# Additional dependencies for your tournament ensemble (updated to meet thrember requirements)
scikit-learn>=1.5.1

# PE file parsing for enhanced feature extraction
pefile>=2023.2.7
